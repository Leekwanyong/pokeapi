export interface KoreaPokemonNameType {
  language: {
    name: string;
    url: string;
  };
  name: string;
}

export interface FlavorTextData {
  id: number;
  flavor_text: string;
}

export interface GenusType {
  id: number;
  genus: string;
}
